<%= form_with url: "#{user_create_hobby_url}", method: :post, width: "50%"  do |form| %>
  <% if hobby.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(hobby.errors.count, "error") %> prohibited this hobby from being saved:</h2>

      <ul>
        <% hobby.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name, style:"text-transform: capitalize;" %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
